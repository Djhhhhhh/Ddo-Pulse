# 上下文摘要

> 自动生成自项目上下文扫描。

## 已加载来源

| 文件路径 | 摘要 |
|---|---|
| README.md | Ddo-Pulse 是本机信息聚合工具：定时抓取博客/订阅源，经 LLM 筛选分类摘要后，支持飞书推送日报和本地 Web Dashboard。技术栈为 Python 3.11+（Typer CLI + FastAPI + SQLite）+ Vue 3 + Vite 前端。数据目录默认 `~/.ddo_pulse`，主配置 `config.yaml`，Web 端口配置 `web.yaml`。启动命令 `ddo-pulse dev`，停止 `ddo-pulse stop`。 |

## 上下文缺失

- AGENTS.md（声明为可选输入，项目根目录不存在此文件）
