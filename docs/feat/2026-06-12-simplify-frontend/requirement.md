# Requirement

现在整体项目有些过于复杂了，我希望简化项目，下面我将从前端展示需要修改的地方：

1. **配置中，模型与密钥** 只能配置 model 和更新 apikey
2. **定时任务中可以创建多个定时任务**，这个保留
3. **定时创建中**：
   - cron 表达式 — **保留**
   - 飞书 Webhook URL — **保留**
   - 阈值 — **删除**
   - 评分侧重点 — 这部分只系统中已经预设好的配置，不允许修改，只给预览如何评分的
   - 抓取与 Digest（可选）— 这部分更新为**必填**，必须进行配置，可以给个默认配置
   - 模型 Profile（可选）— **去掉**
   - 关键词与预过滤（可选）— **保留**
   - 高级提示词（可选）— 修改成可以选择系统中提供好的模板提供预览，然后补充一个点可以让用户输入一段话来表示用户需求，最终的形式应该是用户需求 prompt 和系统 prompt 组合起来然后才是真正的提示词
4. **每个定时任务只允许运行一个任务**
